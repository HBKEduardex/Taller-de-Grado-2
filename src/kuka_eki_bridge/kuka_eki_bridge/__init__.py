"""
kuka_eki_bridge — ROS2 bridge for KUKA EthernetKRL (EKI) communication.

This package implements a TCP/XML server that replaces EthernetKRL_Server.exe
on Ubuntu, allowing direct communication with KUKA robots running XmlTransmit.src
via the KUKA.Ethernet KRL interface.
"""
